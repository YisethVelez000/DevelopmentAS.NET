@model IEnumerable<DevelopmentAS.Models.OrdenProduccion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaEstimada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaEstimada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NroOrdenProduccion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NroOrdenProduccion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NroOrdenProduccion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
