@model IEnumerable<DevelopmentAS.Models.EstampadoProducto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdEstampado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ubicacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Subtotal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdCatalogoPNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdEstampado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ubicacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subtotal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdCatalogoPNavigation.IdcatalogoProducto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdEstampadoProducto">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdEstampadoProducto">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEstampadoProducto">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
