@model DevelopmentAS.Models.Estampado

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Estampado</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdEstampado" />
            <div class="form-group">
                <label asp-for="NombreEstampado" class="control-label"></label>
                <input asp-for="NombreEstampado" class="form-control" />
                <span asp-validation-for="NombreEstampado" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DescripcionEstampado" class="control-label"></label>
                <input asp-for="DescripcionEstampado" class="form-control" />
                <span asp-validation-for="DescripcionEstampado" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="EstadoEstampado" /> @Html.DisplayNameFor(model => model.EstadoEstampado)
                </label>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
