@model IEnumerable<DevelopmentAS.Models.Estampado>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreEstampado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionEstampado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstadoEstampado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreEstampado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionEstampado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EstadoEstampado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdEstampado">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdEstampado">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEstampado">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
